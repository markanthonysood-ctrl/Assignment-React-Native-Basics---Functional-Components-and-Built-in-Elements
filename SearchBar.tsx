import { Text, View } from 'react-native';

const SearchBar = () => {
    return (
        <View style={{alignItems: 'center', padding: 10, backgroundColor: '#eee', marginVertical: 10}}>
            <Text style={{fontSize: 20}}> 🔍 Search Bar </Text>
        </View>
    );
};

export default SearchBar;